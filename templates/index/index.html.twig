<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Kur Karşılaştırma!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}

    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</head>
<body>
{% block body %}
    <div class="container" style="font-size: 30px;">
        <div class="row justify-content-center" style="margin-top: 100px;">
            <table class="table table-striped table-hover col-5">
                <thead class="thead-dark">
                <tr>
                    <th colspan="2" class="text-center">EN UYGUN KUR FİYATLARI</th>
                </tr>
                </thead>
                <tbody>
                {% for result in results %}
                    <tr>
                        <td style="color: red;" colspan="2">{{ result.provider }}</td>
                    </tr>
                    <tr>
                        <td class="text-right font-weight-bold w-50">DOLAR</td>
                        <td>{{ result.dollar }} ₺</td>
                    </tr>
                    <tr>
                        <td class="text-right font-weight-bold w-50">EURO</td>
                        <td>{{ result.euro }} ₺</td>
                    </tr>
                    <tr>
                        <td class="text-right font-weight-bold w-50">STERLIN</td>
                        <td>{{ result.sterling }} ₺</td>
                    </tr>
                {% else %}
                    <tr>
                        <td style="color: red;" colspan="2">Kayıt bulunamadı.</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock %}
{% block javascripts %}{% endblock %}
</body>
</html>
